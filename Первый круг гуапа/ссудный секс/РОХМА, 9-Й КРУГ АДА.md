Проанализировал все предоставленные программы. Вот список **уникальных решений**, убрав дубликаты и объединив варианты с одинаковой логикой:

  

---

  

### **1. Количество строк с отрицательными элементами**

```cpp

#include <iostream>

#include <locale>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n, m;

cout << "Введите n: ";

cin >> n;

cout << "Введите m: ";

cin >> m;

if (n <= 0 || m <= 0) {

cout << "Ошибка: n и m должны быть положительными." << endl;

return 1;

}

double** arr = new double*[n];

for (int i = 0; i < n; i++) arr[i] = new double[m];

for (int i = 0; i < n; i++)

for (int j = 0; j < m; j++) {

cout << "Введите элемент (" << i << ", " << j << "): ";

cin >> arr[i][j];

}

int count = 0;

for (int i = 0; i < n; i++) {

bool hasNegative = false;

for (int j = 0; j < m; j++) {

if (arr[i][j] < 0) {

hasNegative = true;

break;

}

}

if (hasNegative) count++;

}

cout << "Количество строк с отрицательными элементами: " << count << endl;

for (int i = 0; i < n; i++) delete[] arr[i];

delete[] arr;

system("pause");

return 0;

}

```

  

### **2. Массив учеников с определением возраста**

```cpp

#include <iostream>

#include <string>

#include <ctime>

using namespace std;

  

struct Student {

string name, surname, patronymic;

int birthDay, birthMonth, birthYear;

int getAge() {

time_t t = time(nullptr);

tm* current = localtime(&t);

int age = current->tm_year + 1900 - birthYear;

if (current->tm_mon + 1 < birthMonth ||

(current->tm_mon + 1 == birthMonth && current->tm_mday < birthDay)) {

age--;

}

return age;

}

};

  

int main() {

setlocale(LC_ALL, "Russian");

const int n = 3;

Student students[n];

for (int i = 0; i < n; i++) {

cout << "Введите ФИО студента " << i + 1 << " (имя фамилия отчество): ";

cin >> students[i].name >> students[i].surname >> students[i].patronymic;

cout << "Введите дату рождения (день месяц год): ";

cin >> students[i].birthDay >> students[i].birthMonth >> students[i].birthYear;

}

for (int i = 0; i < n; i++) {

cout << students[i].surname << " " << students[i].name << " "

<< students[i].patronymic << " - " << students[i].getAge() << " лет." << endl;

}

system("pause");

return 0;

}

```

  

### **3. Циклический сдвиг строки P на K влево**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n, m, p, k;

cout << "Введите количество строк: ";

cin >> n;

cout << "Введите количество столбцов: ";

cin >> m;

double** arr = new double*[n];

for (int i = 0; i < n; i++) arr[i] = new double[m];

cout << "Введите элементы массива:\n";

for (int i = 0; i < n; i++)

for (int j = 0; j < m; j++) cin >> arr[i][j];

cout << "Введите номер строки для сдвига (от 0): ";

cin >> p;

cout << "Введите величину сдвига K: ";

cin >> k;

k = k % m;

if (k < 0) k += m;

double* temp = new double[k];

for (int i = 0; i < k; i++) temp[i] = arr[p][i];

for (int j = 0; j < m - k; j++) arr[p][j] = arr[p][j + k];

for (int j = 0; j < k; j++) arr[p][m - k + j] = temp[j];

cout << "Массив после сдвига:\n";

for (int i = 0; i < n; i++) {

for (int j = 0; j < m; j++) cout << arr[i][j] << "\t";

cout << endl;

}

for (int i = 0; i < n; i++) delete[] arr[i];

delete[] arr;

delete[] temp;

system("pause");

return 0;

}

```

  

### **4. Строки с max и min суммой элементов**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n, m;

cout << "Введите количество строк: ";

cin >> n;

cout << "Введите количество столбцов: ";

cin >> m;

int** mat = new int*[n];

for (int i = 0; i < n; i++) mat[i] = new int[m];

cout << "Введите элементы матрицы:\n";

for (int i = 0; i < n; i++)

for (int j = 0; j < m; j++) cin >> mat[i][j];

int maxSum = INT_MIN, minSum = INT_MAX;

int maxRow = 0, minRow = 0;

for (int i = 0; i < n; i++) {

int sum = 0;

for (int j = 0; j < m; j++) sum += mat[i][j];

if (sum > maxSum) { maxSum = sum; maxRow = i; }

if (sum < minSum) { minSum = sum; minRow = i; }

}

cout << "Строка с наибольшей суммой (" << maxSum << "): ";

for (int j = 0; j < m; j++) cout << mat[maxRow][j] << " ";

cout << "\nСтрока с наименьшей суммой (" << minSum << "): ";

for (int j = 0; j < m; j++) cout << mat[minRow][j] << " ";

cout << endl;

for (int i = 0; i < n; i++) delete[] mat[i];

delete[] mat;

system("pause");

return 0;

}

```

  

### **5. Преподаватели, читающие заданную дисциплину**

```cpp

#include <iostream>

#include <string>

#include <algorithm>

#include <vector>

using namespace std;

  

struct Teacher {

string surname, name;

vector<string> subjects;

};

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите количество преподавателей: ";

cin >> n;

cin.ignore();

Teacher* teachers = new Teacher[n];

for (int i = 0; i < n; i++) {

cout << "Введите фамилию и имя преподавателя " << i+1 << ": ";

cin >> teachers[i].surname >> teachers[i].name;

cout << "Введите количество дисциплин: ";

int k;

cin >> k;

cin.ignore();

cout << "Введите дисциплины:\n";

for (int j = 0; j < k; j++) {

string subject;

getline(cin, subject);

teachers[i].subjects.push_back(subject);

}

}

string targetSubject;

cout << "Введите дисциплину для поиска: ";

getline(cin, targetSubject);

vector<string> foundSurnames;

for (int i = 0; i < n; i++) {

for (const string& subject : teachers[i].subjects) {

if (subject == targetSubject) {

foundSurnames.push_back(teachers[i].surname);

break;

}

}

}

sort(foundSurnames.begin(), foundSurnames.end());

if (foundSurnames.empty()) {

cout << "Преподаватели не найдены.\n";

} else {

cout << "Преподаватели, читающие '" << targetSubject << "':\n";

for (const string& surname : foundSurnames) cout << surname << endl;

}

delete[] teachers;

system("pause");

return 0;

}

```

  

### **6. Максимальный элемент ниже главной диагонали**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер матрицы n: ";

cin >> n;

double** arr = new double*[n];

for (int i = 0; i < n; i++) arr[i] = new double[n];

cout << "Введите элементы матрицы:\n";

for (int i = 0; i < n; i++)

for (int j = 0; j < n; j++) cin >> arr[i][j];

bool found = false;

double maxElement;

for (int i = 1; i < n; i++) {

for (int j = 0; j < i; j++) {

if (!found || arr[i][j] > maxElement) {

maxElement = arr[i][j];

found = true;

}

}

}

if (found) {

cout << "Максимальный элемент ниже главной диагонали: " << maxElement << endl;

} else {

cout << "Нет элементов ниже главной диагонали.\n";

}

for (int i = 0; i < n; i++) delete[] arr[i];

delete[] arr;

system("pause");

return 0;

}

```

  

### **7. Перевод из 8-ричной в 10-ричную систему**

```cpp

#include <iostream>

#include <string>

#include <cmath>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

string octal;

cout << "Введите число в восьмеричной системе: ";

cin >> octal;

for (char c : octal) {

if (c < '0' || c > '7') {

cout << "Ошибка: не восьмеричное число!\n";

return 1;

}

}

int decimal = 0;

int length = octal.length();

for (int i = 0; i < length; i++) {

int digit = octal[length - 1 - i] - '0';

decimal += digit * pow(8, i);

}

cout << "В десятичной системе: " << decimal << endl;

system("pause");

return 0;

}

```

  

### **8. Страны с минимальной плотностью населения**

```cpp

#include <iostream>

#include <string>

#include <limits>

using namespace std;

  

struct Country {

string name;

long population;

double area;

};

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите количество стран: ";

cin >> n;

Country* countries = new Country[n];

for (int i = 0; i < n; i++) {

cout << "Страна " << i+1 << ":\n";

cout << " Название: "; cin >> countries[i].name;

cout << " Население: "; cin >> countries[i].population;

cout << " Площадь: "; cin >> countries[i].area;

}

double minDensity = numeric_limits<double>::max();

for (int i = 0; i < n; i++) {

double density = countries[i].population / countries[i].area;

if (density < minDensity) minDensity = density;

}

cout << "Страны с минимальной плотностью (" << minDensity << " чел/км²):\n";

for (int i = 0; i < n; i++) {

if (countries[i].population / countries[i].area == minDensity) {

cout << " " << countries[i].name << endl;

}

}

delete[] countries;

system("pause");

return 0;

}

```

  

### **9. Сумма положительных элементов четных строк**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int rows, cols;

cout << "Введите количество строк: ";

cin >> rows;

cout << "Введите количество столбцов: ";

cin >> cols;

double** array = new double*[rows];

for (int i = 0; i < rows; i++) array[i] = new double[cols];

cout << "Введите элементы массива:\n";

for (int i = 0; i < rows; i++)

for (int j = 0; j < cols; j++) cin >> array[i][j];

double sum = 0.0;

bool found = false;

for (int i = 1; i < rows; i += 2) {

for (int j = 0; j < cols; j++) {

if (array[i][j] > 0) {

sum += array[i][j];

found = true;

}

}

}

if (found) {

cout << "Сумма положительных элементов четных строк: " << sum << endl;

} else {

cout << "В четных строках нет положительных элементов.\n";

}

for (int i = 0; i < rows; i++) delete[] array[i];

delete[] array;

system("pause");

return 0;

}

```

  

### **10. Количество строк в файле**

```cpp

#include <iostream>

#include <fstream>

#include <string>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

string filename;

cout << "Введите путь к файлу: ";

getline(cin, filename);

ifstream file(filename);

if (!file.is_open()) {

cout << "Ошибка открытия файла!\n";

return 1;

}

int lineCount = 0;

string line;

while (getline(file, line)) lineCount++;

cout << "Количество строк: " << lineCount << endl;

file.close();

system("pause");

return 0;

}

```

  

### **11. Замена второго вхождения подстроки**

```cpp

#include <iostream>

#include <string>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

string st, st1, st2;

cout << "Введите основную строку: ";

getline(cin, st);

cout << "Введите подстроку для поиска: ";

getline(cin, st1);

cout << "Введите подстроку для замены: ";

getline(cin, st2);

size_t firstPos = st.find(st1);

if (firstPos == string::npos) {

cout << "Подстрока не найдена.\n";

} else {

size_t secondPos = st.find(st1, firstPos + 1);

if (secondPos == string::npos) {

cout << "Второе вхождение не найдено.\n";

} else {

st.replace(secondPos, st1.length(), st2);

cout << "Результат: " << st << endl;

}

}

system("pause");

return 0;

}

```

  

### **12. Трехзначные числа, где сумма цифр = 7 и число кратно 7**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

for (int n = 100; n < 1000; n++) {

int sum = (n / 100) + ((n / 10) % 10) + (n % 10);

if (sum == 7 && n % 7 == 0) {

cout << n << " ";

}

}

cout << endl;

system("pause");

return 0;

}

```

  

### **13. Студенты, родившиеся летом**

```cpp

#include <iostream>

#include <string>

using namespace std;

  

struct Student {

string surname, name, patronymic;

int day, month, year;

};

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите количество студентов: ";

cin >> n;

Student* students = new Student[n];

for (int i = 0; i < n; i++) {

cout << "Студент " << i+1 << ":\n";

cout << " Фамилия: "; cin >> students[i].surname;

cout << " Имя: "; cin >> students[i].name;

cout << " Отчество: "; cin >> students[i].patronymic;

cout << " Дата рождения (день месяц год): ";

cin >> students[i].day >> students[i].month >> students[i].year;

}

cout << "Родились летом (июнь-август):\n";

bool found = false;

for (int i = 0; i < n; i++) {

if (students[i].month >= 6 && students[i].month <= 8) {

cout << " " << students[i].surname << endl;

found = true;

}

}

if (!found) cout << " Не найдено.\n";

delete[] students;

system("pause");

return 0;

}

```

  

### **14. Количество максимальных элементов в массиве**

```cpp

#include <iostream>

using namespace std;

  

int countMaxElements(double* arr, int size) {

if (size == 0) return 0;

double max = arr[0];

int count = 1;

for (int i = 1; i < size; i++) {

if (arr[i] > max) {

max = arr[i];

count = 1;

} else if (arr[i] == max) {

count++;

}

}

return count;

}

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер массива: ";

cin >> n;

double* arr = new double[n];

cout << "Введите элементы:\n";

for (int i = 0; i < n; i++) cin >> arr[i];

cout << "Количество максимальных элементов: " << countMaxElements(arr, n) << endl;

delete[] arr;

system("pause");

return 0;

}

```

  

### **15. Сумма совершенных чисел в массиве**

```cpp

#include <iostream>

using namespace std;

  

bool isPerfect(int n) {

if (n <= 1) return false;

int sum = 1;

for (int i = 2; i * i <= n; i++) {

if (n % i == 0) {

sum += i;

if (i != n / i) sum += n / i;

}

}

return sum == n;

}

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер массива: ";

cin >> n;

int* arr = new int[n];

cout << "Введите элементы:\n";

for (int i = 0; i < n; i++) cin >> arr[i];

int perfectSum = 0;

for (int i = 0; i < n; i++) {

if (isPerfect(arr[i])) perfectSum += arr[i];

}

cout << "Сумма совершенных чисел: " << perfectSum << endl;

delete[] arr;

system("pause");

return 0;

}

```

  

### **16. Уникальные буквы в алфавитном порядке**

```cpp

#include <iostream>

#include <string>

#include <algorithm>

#include <cctype>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

string text;

cout << "Введите текст: ";

getline(cin, text);

int counts[256] = {0};

for (char c : text) {

if (isalpha(c)) counts[tolower(c)]++;

}

bool found = false;

cout << "Буквы, встречающиеся один раз:\n";

for (int i = 0; i < 256; i++) {

if (counts[i] == 1) {

cout << (char)i << " ";

found = true;

}

}

if (!found) cout << "Нет таких букв.";

cout << endl;

system("pause");

return 0;

}

```

  

### **17. Извлечение и сортировка цифр из текста**

```cpp

#include <iostream>

#include <string>

#include <algorithm>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

string text;

cout << "Введите текст: ";

getline(cin, text);

string digits;

for (char c : text) {

if (isdigit(c)) digits += c;

}

sort(digits.begin(), digits.end());

digits.erase(unique(digits.begin(), digits.end()), digits.end());

if (digits.empty()) {

cout << "Цифр не найдено.\n";

} else {

cout << "Уникальные цифры: " << digits << endl;

}

system("pause");

return 0;

}

```

  

### **18. Целые отрицательные элементы массива**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер массива: ";

cin >> n;

double* arr = new double[n];

cout << "Введите элементы:\n";

for (int i = 0; i < n; i++) cin >> arr[i];

cout << "Целые отрицательные элементы:\n";

bool found = false;

for (int i = 0; i < n; i++) {

if (arr[i] < 0 && (int)arr[i] == arr[i]) {

cout << "arr[" << i << "] = " << arr[i] << endl;

found = true;

}

}

if (!found) cout << "Не найдены.\n";

delete[] arr;

system("pause");

return 0;

}

```

  

### **19. Перемещение положительных в начало**

```cpp

#include <iostream>

using namespace std;

  

void movePositive(double* arr, int n) {

int j = 0;

for (int i = 0; i < n; i++) {

if (arr[i] > 0) {

swap(arr[i], arr[j]);

j++;

}

}

}

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер массива: ";

cin >> n;

double* arr = new double[n];

cout << "Введите элементы:\n";

for (int i = 0; i < n; i++) cin >> arr[i];

movePositive(arr, n);

cout << "После перемещения:\n";

for (int i = 0; i < n; i++) cout << arr[i] << " ";

cout << endl;

delete[] arr;

system("pause");

return 0;

}

```

  

### **20. Бьет ли ферзь фигуру**

```cpp

#include <iostream>

#include <cmath>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

char qx, fx;

int qy, fy;

cout << "Введите позицию ферзя (буква цифра): ";

cin >> qx >> qy;

cout << "Введите позицию фигуры (буква цифра): ";

cin >> fx >> fy;

qx = tolower(qx);

fx = tolower(fx);

if (qx == fx || qy == fy || abs(qx - fx) == abs(qy - fy)) {

cout << "Ферзь бьет фигуру.\n";

} else {

cout << "Ферзь не бьет фигуру.\n";

}

system("pause");

return 0;

}

```

  

### **21. Бьет ли конь фигуру**

```cpp

#include <iostream>

#include <cmath>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

char kx, fx;

int ky, fy;

cout << "Введите позицию коня (буква цифра): ";

cin >> kx >> ky;

cout << "Введите позицию фигуры (буква цифра): ";

cin >> fx >> fy;

kx = tolower(kx);

fx = tolower(fx);

int dx = abs(kx - fx);

int dy = abs(ky - fy);

if ((dx == 1 && dy == 2) || (dx == 2 && dy == 1)) {

cout << "Конь бьет фигуру.\n";

} else {

cout << "Конь не бьет фигуру.\n";

}

system("pause");

return 0;

}

```

  

### **22. Минимальный положительный элемент**

```cpp

#include <iostream>

#include <limits>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер массива: ";

cin >> n;

double* arr = new double[n];

cout << "Введите элементы:\n";

for (int i = 0; i < n; i++) cin >> arr[i];

double minPos = numeric_limits<double>::max();

bool found = false;

for (int i = 0; i < n; i++) {

if (arr[i] > 0 && arr[i] < minPos) {

minPos = arr[i];

found = true;

}

}

if (found) {

cout << "Минимальный положительный элемент: " << minPos << endl;

} else {

cout << "Положительных элементов нет.\n";

}

delete[] arr;

system("pause");

return 0;

}

```

  

### **23. Дата следующего дня**

```cpp

#include <iostream>

using namespace std;

  

bool isLeap(int year) {

return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);

}

  

int main() {

setlocale(LC_ALL, "Russian");

int day, month, year;

cout << "Введите дату (день месяц год): ";

cin >> day >> month >> year;

int daysInMonth[] = {31, 28 + isLeap(year), 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};

day++;

if (day > daysInMonth[month - 1]) {

day = 1;

month++;

if (month > 12) {

month = 1;

year++;

}

}

cout << "Следующий день: " << day << "." << month << "." << year << endl;

system("pause");

return 0;

}

```

  

### **24. Произведение после последнего минимального**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер массива: ";

cin >> n;

double* arr = new double[n];

cout << "Введите элементы:\n";

for (int i = 0; i < n; i++) cin >> arr[i];

int lastMinIndex = 0;

for (int i = 1; i < n; i++) {

if (arr[i] <= arr[lastMinIndex]) lastMinIndex = i;

}

double product = 1.0;

bool found = false;

for (int i = lastMinIndex + 1; i < n; i++) {

product *= arr[i];

found = true;

}

if (found) {

cout << "Произведение после последнего минимального: " << product << endl;

} else {

cout << "Элементов после последнего минимального нет.\n";

}

delete[] arr;

system("pause");

return 0;

}

```

  

### **25. Слова-палиндромы**

```cpp

#include <iostream>

#include <string>

#include <algorithm>

using namespace std;

  

bool isPalindrome(string word) {

string reversed = word;

reverse(reversed.begin(), reversed.end());

return word == reversed;

}

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите количество слов: ";

cin >> n;

cin.ignore();

string* words = new string[n];

cout << "Введите слова:\n";

for (int i = 0; i < n; i++) getline(cin, words[i]);

cout << "Палиндромы:\n";

bool found = false;

for (int i = 0; i < n; i++) {

if (isPalindrome(words[i])) {

cout << words[i] << endl;

found = true;

}

}

if (!found) cout << "Не найдены.\n";

delete[] words;

system("pause");

return 0;

}

```

  

### **26. Матрица по образцу**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер матрицы: ";

cin >> n;

int** mat = new int*[n];

for (int i = 0; i < n; i++) mat[i] = new int[n]{0};

for (int i = 0; i < n; i++) {

mat[i][i] = i + 1;

mat[i][n - i - 1] = n - i;

}

cout << "Матрица:\n";

for (int i = 0; i < n; i++) {

for (int j = 0; j < n; j++) {

cout << mat[i][j] << "\t";

}

cout << endl;

}

for (int i = 0; i < n; i++) delete[] mat[i];

delete[] mat;

system("pause");

return 0;

}

```

  

### **27. Слова с нечетным количеством слогов**

```cpp

#include <iostream>

#include <string>

using namespace std;

  

int countSyllables(string word) {

int count = 0;

string vowels = "aeiouyаеёиоуыэюяAEIOUYАЕЁИОУЫЭЮЯ";

for (char c : word) {

if (vowels.find(c) != string::npos) count++;

}

return count;

}

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите количество слов: ";

cin >> n;

cin.ignore();

string* words = new string[n];

cout << "Введите слова:\n";

for (int i = 0; i < n; i++) getline(cin, words[i]);

cout << "Слова с нечетным количеством слогов:\n";

bool found = false;

for (int i = 0; i < n; i++) {

if (countSyllables(words[i]) % 2 == 1) {

cout << words[i] << endl;

found = true;

}

}

if (!found) cout << "Не найдены.\n";

delete[] words;

system("pause");

return 0;

}

```

  

### **28. Увеличение нечетных столбцов на min диагонали**

```cpp

#include <iostream>

using namespace std;

  

int main() {

setlocale(LC_ALL, "Russian");

int n;

cout << "Введите размер матрицы: ";

cin >> n;

double** mat = new double*[n];

for (int i = 0; i < n; i++) mat[i] = new double[n];

cout << "Введите элементы:\n";

for (int i = 0; i < n; i++)

for (int j = 0; j < n; j++) cin >> mat[i][j];

double minDiag = mat[0][0];

for (int i = 1; i < n; i++) {

if (mat[i][i] < minDiag) minDiag = mat[i][i];

}

for (int j = 1; j < n; j += 2) {

for (int i = 0; i < n; i++) {

mat[i][j] += minDiag;

}

}

cout << "Результат:\n";

for (int i = 0; i < n; i++) {

for (int j = 0; j < n; j++) {

cout << mat[i][j] << "\t";

}

cout << endl;

}

for (int i = 0; i < n; i++) delete[] mat[i];

delete[] mat;

system("pause");

return 0;

}

```

  

---

  

**Итого:** Получилось **28 уникальных программ**, охватывающих все задачи из оглавления без дублирования. Каждая программа решает свою конкретную задачу.